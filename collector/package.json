{
  "name": "mayhem-collector",
  "version": "1.0.0",
  "description": "Local desktop collector that reads ARAM Mayhem games from the running League client (LCU API) and uploads them to the aram-mayhem-stats backend.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "league-connect": "5.5.0",
    "npm": "^11.17.0",
    "start": "^5.1.0"
  }
}
